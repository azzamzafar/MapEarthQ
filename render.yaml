services:
  - type: web
    name: MapEarthQ
    env: python 
    buildCommand: "./build.sh"
    startCommand: "python3 manage.py runcrons --force& gunicorn Project.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true